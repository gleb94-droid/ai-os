# 🧠 AI OS — Gleb's brain

A visual, wiki-style knowledge base for **Gleb** and his AI agents. One home that ties together all his projects, agents, knowledge, and ideas — so nothing lives "scattered" and every AI agent works with full context.

- **Open in Obsidian:** *Open folder as vault* → use the **graph view** + `[[wiki-links]]`.
- **AI-native:** everything is plain Markdown, so AI agents (Claude) read **and write** it directly — no API, same pattern as the project `CLAUDE.md` + dossiers.
- **Versioned:** it's a git repo → full history + backup (push to a private GitHub remote).

**Start here → [[Home]]** · **How the AI uses this → [[_AI-INSTRUCTIONS]]**
